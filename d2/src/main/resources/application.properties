spring.application.name=d2
# H2 Database Connection Settings
spring.datasource.url=jdbc:h2:mem:company_db
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Hibernate Settings (ORM)
# ddl-auto=update: Tells Hibernate to generate tables based on your classes
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Print SQL Queries to the Terminal for debugging
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true